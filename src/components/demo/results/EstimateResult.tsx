import { StatusBadge } from '../StatusBadge';
import { MetricCard } from '../MetricCard';
import { DataTable } from '../DataTable';
import { ActionButton, ActionButtonGroup } from '../workspace/ActionButton';
import { DataSourceBadges } from '../workspace/DataSourceBadges';
import { PartDrawingPreview, SpecSheetPreview } from '../workspace/FilePreviewCards';
import { DATA_SOURCES_BY_RESULT } from '../../../data/demoWorkspace';

interface EstimateResultProps {
  prompt: string;
  completedActions: Set<string>;
  onAction: (id: string) => void;
}

export function EstimateResult({ completedActions, onAction }: EstimateResultProps) {
  return (
    <div className="space-y-4">
      <div className="flex flex-wrap items-center justify-between gap-2">
        <h3 className="text-lg font-semibold text-white">Sensor Module X · 500 units</h3>
        <StatusBadge variant="ai">Generated by AI</StatusBadge>
      </div>

      <div className="grid gap-4 lg:grid-cols-2">
        <PartDrawingPreview
          filename="drawing.tif"
          title="Part Drawing"
          alt="Sensor Module X part drawing"
        />
        <SpecSheetPreview
          filename="specs.xlsx"
          title="Tool Build Spec"
          alt="Sensor Module X tool build specification"
        />
      </div>

      <div className="grid gap-4 lg:grid-cols-2">
        <div className="rounded-xl border border-neutral-800 bg-[#0a0a0a] p-4">
          <p className="mb-3 text-[10px] font-semibold uppercase tracking-wider text-neutral-500">BOM</p>
          <DataTable
            columns={[
              { key: 'component', header: 'Component', className: 'text-white' },
              { key: 'qty', header: 'Qty', align: 'right' },
              { key: 'cost', header: 'Cost', align: 'right' },
            ]}
            rows={[
              { component: 'PCB Board A', qty: '1', cost: '£8.40' },
              { component: 'Aluminium Housing', qty: '1', cost: '£6.20' },
              { component: 'Sensor chip', qty: '1', cost: '£4.10' },
              { component: 'Fasteners', qty: '1 set', cost: '£2.60' },
            ]}
            minWidth="280px"
          />
        </div>
        <div className="rounded-xl border border-neutral-800 bg-[#0a0a0a] p-4">
          <p className="mb-3 text-[10px] font-semibold uppercase tracking-wider text-neutral-500">Routing</p>
          <DataTable
            columns={[
              { key: 'op', header: 'Operation', className: 'text-white' },
              { key: 'machine', header: 'Machine' },
              { key: 'time', header: 'Time', align: 'right' },
            ]}
            rows={[
              { op: 'PCB assembly', machine: 'ASM-01', time: '12 min' },
              { op: 'Housing fitment', machine: 'ASM-01', time: '8 min' },
              { op: 'Calibration', machine: 'QA-03', time: '6 min' },
            ]}
            minWidth="280px"
          />
        </div>
      </div>

      <div className="grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-6">
        <MetricCard label="Unit cost" value="£42.80" />
        <MetricCard label="Price" value="£61.00" valueClassName="text-emerald-400" />
        <MetricCard label="Margin" value="29.8%" valueClassName="text-emerald-400" />
        <MetricCard label="Total" value="£30,500" />
        <MetricCard label="Lead time" value="18 days" />
        <MetricCard label="Confidence" value="91%" valueClassName="text-violet-400" />
      </div>

      <div className="rounded-xl border border-violet-500/20 bg-violet-500/5 p-4">
        <p className="mb-2 text-[10px] font-semibold uppercase tracking-wider text-violet-400">Cost model</p>
        <div className="grid grid-cols-2 gap-x-4 gap-y-1 text-sm sm:grid-cols-3">
          {[
            ['Materials', '£21.30/unit'],
            ['Machine time', '£8.40/unit'],
            ['Labour', '£6.20/unit'],
            ['Testing', '£3.10/unit'],
            ['Overhead', '£3.80/unit'],
          ].map(([k, v]) => (
            <div key={k} className="flex justify-between gap-2">
              <span className="text-neutral-400">{k}</span>
              <span className="text-white">{v}</span>
            </div>
          ))}
        </div>
      </div>

      <DataSourceBadges sources={DATA_SOURCES_BY_RESULT.estimating} />

      <ActionButtonGroup>
        <ActionButton
          id="estimating-tool"
          label="Turn into Reusable Estimating Tool"
          variant="primary"
          completed={completedActions.has('estimating-tool')}
          completedLabel="Tool Created"
          successMessage="Sensor Module X estimating tool saved for future RFQs."
          onClick={onAction}
        />
        <ActionButton
          id="quote-draft"
          label="Generate Quote Draft"
          completed={completedActions.has('quote-draft')}
          completedLabel="Quote Draft Created"
          onClick={onAction}
        />
      </ActionButtonGroup>
    </div>
  );
}
