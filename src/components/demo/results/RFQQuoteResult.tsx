import { Paperclip } from 'lucide-react';
import { StatusBadge } from '../StatusBadge';
import { MetricCard } from '../MetricCard';
import { ActionButton, ActionButtonGroup } from '../workspace/ActionButton';
import { DataSourceBadges } from '../workspace/DataSourceBadges';
import { PartDrawingPreview, SpecSheetPreview } from '../workspace/FilePreviewCards';
import { DATA_SOURCES_BY_RESULT } from '../../../data/demoWorkspace';

interface RFQQuoteResultProps {
  completedActions: Set<string>;
  onAction: (id: string) => void;
}

export function RFQQuoteResult({ completedActions, onAction }: RFQQuoteResultProps) {
  return (
    <div className="space-y-4">
      <div className="flex flex-wrap items-center justify-between gap-2">
        <h3 className="text-lg font-semibold text-white">Schneider Electric RFQ → Quote Draft</h3>
        <StatusBadge variant="ai">Generated by AI</StatusBadge>
      </div>

      <div className="rounded-xl border border-neutral-800 bg-[#0a0a0a] p-4">
        <div className="mb-3 flex flex-wrap items-center justify-between gap-2 text-xs text-neutral-500">
          <span className="uppercase tracking-wider">RFQ Inbox</span>
          <span>Today 09:04</span>
        </div>
        <div className="mb-3 space-y-1 font-mono text-xs text-neutral-400">
          <p>
            <span className="text-neutral-500">From:</span> procurement@schneider-electric.com
          </p>
          <p>
            <span className="text-neutral-500">Subject:</span>{' '}
            <span className="text-white">RFQ — Custom Aluminium Housing · 2,000 units</span>
          </p>
        </div>
        <div className="mb-3 flex flex-wrap gap-2">
          <span className="inline-flex items-center gap-1.5 rounded border border-neutral-800 bg-neutral-900 px-2 py-1 text-xs text-neutral-300">
            <Paperclip className="h-3 w-3" />
            SE-HOUSING-4421.pdf
          </span>
          <span className="inline-flex items-center gap-1.5 rounded border border-neutral-800 bg-neutral-900 px-2 py-1 text-xs text-neutral-300">
            <Paperclip className="h-3 w-3" />
            specs.xlsx
          </span>
        </div>
        <p className="text-sm text-neutral-400">
          Quote custom aluminium housing per attached drawing and spec. Delivery required in 4 weeks.
        </p>
      </div>

      <div className="grid gap-4 lg:grid-cols-2">
        <PartDrawingPreview
          filename="SE-HOUSING-4421.pdf"
          title="Part Drawing"
          alt="SE-HOUSING-4421 part drawing for Schneider Electric"
        />
        <SpecSheetPreview
          filename="specs.xlsx"
          title="Tool Build Spec"
          alt="Tool build specification for SE-HOUSING-4421"
        />
      </div>

      <div className="grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-6">
        <MetricCard label="Unit cost" value="£18.40" />
        <MetricCard label="Recommended price" value="£27.50" valueClassName="text-emerald-400" />
        <MetricCard label="Gross margin" value="33.1%" valueClassName="text-emerald-400" />
        <MetricCard label="Total quote" value="£55,000" />
        <MetricCard label="Lead time" value="23 days" />
        <MetricCard label="Confidence" value="87%" valueClassName="text-violet-400" />
      </div>

      <div className="rounded-xl border border-neutral-800 bg-[#0a0a0a] p-4">
        <p className="mb-2 text-[10px] font-semibold uppercase tracking-wider text-neutral-500">
          Quote draft · Q-SE-2847
        </p>
        <div className="grid grid-cols-2 gap-x-4 gap-y-1 text-sm sm:grid-cols-3">
          {[
            ['Material', '£8.20/unit'],
            ['Machine time', '£4.70/unit'],
            ['Labour', '£2.90/unit'],
            ['Finishing', '£1.80/unit'],
            ['Overhead', '£0.80/unit'],
          ].map(([k, v]) => (
            <div key={k} className="flex justify-between gap-2">
              <span className="text-neutral-500">{k}</span>
              <span className="text-white">{v}</span>
            </div>
          ))}
        </div>
      </div>

      <DataSourceBadges sources={DATA_SOURCES_BY_RESULT['rfq-quote']} />

      <ActionButtonGroup>
        <ActionButton
          id="quote-pdf"
          label="Generate Quote PDF"
          variant="primary"
          completed={completedActions.has('quote-pdf')}
          completedLabel="Quote PDF Created"
          onClick={onAction}
        />
        <ActionButton
          id="sales-review"
          label="Send to Commercial Review"
          completed={completedActions.has('sales-review')}
          completedLabel="Sent for Review"
          onClick={onAction}
        />
      </ActionButtonGroup>
    </div>
  );
}
